recup_groups_user_modified($id_user,$time) //Renvoie un tableau associatif avec les groupes de l'Utilisateur modifiés depuis le temps en parametres

recup_file_filename($filename) //Renvoie un tableau associatif avec les attributs du fichier 
recup_file_id($id) //Renvoie un tableau associatif avec les attributs du fichier
recup_files_modified($id_folder,$time) //Renvoie un tableau associatif avec les fichiers modifiés depuis le temps en parametres dans le dossier concerné

supprime_fichier($id)
create_file($folder,$filename,$content_type,$size,$descr)//Crée un fichier avec les parametres
modifie_file($id,$nom,$description) // Modifie le fichier avec un nouveua nom et description

recup_folder_id($id) //Renvoie un tableau associatif avec les attributs du dossier 
create_folder($nom,$parent,$description) //Crée le dossier puis renvoyer un bool
recup_folder_nom_descr($nom,$description) // Renvoie l'id du folder, null sinon

Penser a modifier les lastUpdates !!!
Time/lastUpdates : fixer une valeur ?

Retour Utilisateur :
Inscription -> aucune validation
Si session demarre vide, Erreur
pas de seconde tentative